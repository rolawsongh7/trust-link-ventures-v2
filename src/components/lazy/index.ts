/**
 * Lazy-Loaded Heavy Components
 * Reduces initial bundle size by loading components on demand
 * 
 * Add your heavy components here for lazy loading:
 * 
 * Example:
 * export const HeavyChart = lazyLoadComponent(() => import('@/components/charts/HeavyChart'));
 */

import { lazyLoadComponent } from '@/utils/lazyLoad';

// Placeholder for future lazy-loaded components
// Add components as they are created
export const placeholder = null;
